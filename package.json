{
  "name": "basil",
  "version": "1.0.0",
  "description": "notify when the basil plant needs water",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "stylus public/css/style.styl --compress --out public --watch",
    "sample": "node index sample",
    "deploy": "git push production master && rhc app-force-stop --app basil && rhc deploy master --app basil",
    "start": "node index",
    "log": "rhc tail -o '-n 200' -a basil",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ssh": "rhc ssh --app basil"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^2.0.0",
    "eventsource": "^0.2.1",
    "express": "^4.13.4",
    "firebase": "^2.2.1",
    "jade": "^1.11.0",
    "lodash": "^4.9.0",
    "moment-timezone": "^0.5.1",
    "morgan": "^1.7.0",
    "request": "^2.69.0",
    "socket.io": "^1.4.5",
    "tracer": "^0.8.3"
  },
  "devDependancies": {
    "dotenv": "^2.0.0",
    "stylus": "^0.54.2"
  }
}
