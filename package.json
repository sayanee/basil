{
  "name": "cosmic",
  "version": "1.0.0",
  "description": "notify when the basil plant needs water",
  "main": "index.js",
  "private": true,
  "scripts": {
    "deploy": "git push production master",
    "start": "node index",
    "log": "rhc tail -o '-n 200' -a basil",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^2.0.0",
    "eventsource": "^0.2.1",
    "express": "^4.13.4",
    "jade": "^1.11.0",
    "moment": "^2.12.0",
    "request": "^2.69.0",
    "tracer": "^0.8.3"
  },
  "devDependancies": {
    "dotenv": "^2.0.0"
  }
}
